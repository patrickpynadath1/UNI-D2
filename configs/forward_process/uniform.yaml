# Hydra instantiation config for Uniform forward process
# Note: tokenizer and schedule must be passed at runtime
_target_: discrete_diffusion.forward_process.UniformForwardProcess

# Legacy name field for backward compatibility
name: uniform

