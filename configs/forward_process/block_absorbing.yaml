# Hydra instantiation config for Block Absorbing forward process
# Note: tokenizer and schedule must be passed at runtime
_target_: discrete_diffusion.forward_process.BlockAbsorbingForwardProcess

# Legacy name field for backward compatibility
name: block_absorbing

